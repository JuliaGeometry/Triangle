#ifndef TRIWRAPJULIA_H
extern void triexit(int);
extern int triangulate_catch_exit(char *triswitches,
                                  struct triangulateio *in,
                                  struct triangulateio *out,
                                  struct triangulateio *vorout);
  
#endif
